services:
  - type: web
    name: voice-recognition-system-app
    runtime: python
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fastapi_main:app --host 0.0.0.0 --port 10000
